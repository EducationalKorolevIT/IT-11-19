#include <iostream>

using namespace std;

int main()
{
    int a = 0, b = 0;

    cin >> a >> b;

    cout << (a*a)+(b*b) << endl;
    cout << (a*a)-(b*b) << endl;
    cout << (a*a)*(b*b) << endl;
    cout << (a*a)/(b*b) << endl;
    return 0;
}
